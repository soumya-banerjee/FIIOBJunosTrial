import React from 'react';

const FrontLogo = (props) => (
      <div>
        <h2 className="logo-name">{props.logoname}</h2>
        <h2 className="subtitle">{props.subtitle}</h2>
      </div>
    );


export default FrontLogo;
