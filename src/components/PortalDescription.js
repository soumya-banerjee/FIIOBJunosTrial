import React from 'react';

const PortalDescription = (props) => (
      <div style={{marginTop: '80px', marginBottom: '80px', textShadow: '1px 1px 2px #000'}}>
        {props.children}
      </div>
    );


export default PortalDescription;
